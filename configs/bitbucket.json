{
    "title": "Bitbucket review",
    "page": "http://bitbucket.rd.800best.com/dashboard",
    "desc": "自己创建的 review",
    "url": "http://bitbucket.rd.800best.com/rest/api/latest/dashboard/pull-requests?avatarSize=64&order=participant_status&limit=25&state=OPEN&role=author",
    "type": "json",
    "method": "GET",
    "parserCode": "const json = html;\nconst list = json.values;\nreturn list.map(item => {\n return {\n key: item.id,\n title: item.title,\n message: item.description,\n };\n});",
    "notifyCode": "// 根据变更类型发通知，并且每条变更都发一条通知。\nconst { addedNodes, updatedNodes, removedNodes } = updates;\nconst notifications = [];\nif (removedNodes.length > 0) {\n notifications.push(...removedNodes.map((node) => {\n return {\n title: 'review 通过了',\n message: node.title,\n };\n }));\n}\nreturn notifications;",
    "sleep": 5000
}